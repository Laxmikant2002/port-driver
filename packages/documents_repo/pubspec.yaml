name: documents_repo
description: "Documents repository for driver app document management"
version: 0.0.1
homepage: https://github.com/your-org/documents_repo

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  api_client:
    path: ../api_client
  localstorage:
    path: ../localstorage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
